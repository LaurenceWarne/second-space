/**
 * Classes in this package are used for resolving 'data' components into 'world' components which don't support serialization. Typically the data components serve no purpose in the world except to save the relevant state of the component types they shadow for serialization.
 */
package laurencewarne.secondspace.common.system.resolvers;

